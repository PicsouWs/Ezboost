{/* Previous imports remain the same */}

const STRIPE_PUBLISHABLE_KEY = 'pk_test_BXaLvA2JfjPYXg7H4IMKRNbW00KhJlINIn';

export default function PaymentModal({ isOpen, onClose, amount, currentRank, desiredRank }: PaymentModalProps) {
  // Rest of the component remains the same
}